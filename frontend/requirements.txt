# Jarvis – Python-Abhängigkeiten für Aktions-Server (tools/action-server.py) und Whisper-STT.
# Wird von setup.bat installiert (bzw. `pip install -r frontend/requirements.txt`).
# Fehlt ein Paket doch einmal, degradiert das betroffene Feature sauber (siehe GET /capabilities).

psutil            # Systeminfo (CPU/RAM/Akku/Disk), Netzwerk
pillow            # Screenshots + Skalierung (Vision, see_screen)
mss               # schnellere Screenshots (Alternative zu Pillow ImageGrab)
pyautogui         # Maus/Tastatur-Steuerung, autonomer Agent-Loop (agent_task)
pyperclip         # Zwischenablage lesen/schreiben
pygetwindow       # Fensterliste / Fenster-Management (optional)

faster-whisper    # lokales STT (Offline-Spracherkennung, tools/stt-server.py)
                  # Die App sendet WAV – ffmpeg ist NICHT nötig. Modell wird autom. geladen.

# TTS (XTTS-v2) und Z-Image laufen in eigenen venvs (tools/tts-venv, tools/zimage-venv);
# setup.bat legt beide an – Details im README.
